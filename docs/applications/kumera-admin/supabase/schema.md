# Kumera Admin Schema Touchpoints

Last updated: 2026-03-03

Kumera Admin is cross-domain and does not own a dedicated schema.

Primary touchpoints:
- `core` (roles and global admin authorization)
- `billing` (billing operations)
- `tuejecutiva` and `leados` (domain administration)

Any new admin capability must document which schema/tables it touches.
