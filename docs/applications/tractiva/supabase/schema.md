# Tractiva Schema Status

Last updated: 2026-03-03

Current state:
- No dedicated `tractiva` schema migration reference exists yet.
- Tractiva commercial/onboarding contractual artifacts are handled through billing references.

If Tractiva gets its own schema later:
1. Add reference SQL file under `docs/applications/tractiva/supabase/migrations/`.
2. Update this file with table contract.
3. Update `docs/monorepo/source-of-truth.md` schema map.
